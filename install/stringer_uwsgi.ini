[uwsgi]
base = /var/www/stringer

app = permutations_app
module = %(app)

home = %(base)
pythonpath = %(base)

socket = /var/www/stringer/install/%n.sock

chmod-socket    = 666

callable = app

logto = /var/log/uwsgi/%n.log